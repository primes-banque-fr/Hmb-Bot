services:
  - type: web
    name: hmb-ai-bot
    env: python
    plan: free

    buildCommand: |
      pyenv install 3.11.9
      pyenv global 3.11.9
      pip install -r requirements.txt

    startCommand: python main.py

    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
